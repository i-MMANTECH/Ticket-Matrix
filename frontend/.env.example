# Emmanuel Aro's project submission for evaluation.
# Copy to frontend/.env.local

# Public base URL of the Django REST API (host-side address used by the browser)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api
